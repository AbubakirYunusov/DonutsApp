<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Base.Theme.DonutsApp" parent="Theme.Material3.DayNight.NoActionBar">
        <!-- Customize your dark theme here. -->
        <!-- <item name="colorPrimary">@color/my_dark_primary</item> -->
        <item name="android:statusBarColor">@color/onbording_beckraund</item>
        <item name="android:navigationBarColor">@color/onbording_beckraund</item>|
    </style>
    <style name="Theme.FoodApp" parent="Base.Theme.DonutsApp">
        <item name="foodBackground">@color/white</item>
        <item name="foodBottonBackground">@color/white</item>
        <item name="foodBottonTextColor">@color/black</item>
        <item name="foodTextColor">@color/black</item>
    </style>
</resources>